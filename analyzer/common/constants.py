import os

class Constants(object):
	""" Constants file """

	# Directories path
	# PATH_ROOT_DIR = os.path.join(os.path.dirname(__file__),'..','..')
	# PATH_ANALYZER_DIR = os.path.join(PATH_ROOT_DIR, 'analyzer')
	# PATH_DATASETS_DIR = os.path.join(PATH_ROOT_DIR, 'datasets')
